import React from 'react';

export function Header() {
    return (
        <header style={{ padding: 16, backgroundColor: 'rgba(0,0,0,0.1)' }}>
            Lerna is the original monorepo tool! from @shadowvzs/header
        </header>
    );
}
